extends /_layout/_default-layout

append config-block
  - worksPath = ''

block content-block
  +page-title

  .inner-block
    .list
      .item
        a(href=`${worksPath}web/`)
          .card.mdl-card.mdl-shadow--2dp(style=
            `background-image: url(${rootPath}img/works/thumb/web.png);`
          )

            .mdl-card__title.mdl-card--expand
            .mdl-card__actions
              .title
                span
                  | Web

      .item
        a(href=`${worksPath}design/`)
          .card.mdl-card.mdl-shadow--2dp(style=
            `background-image: url(${rootPath}img/works/thumb/design.png);`
          )

            .mdl-card__title.mdl-card--expand
            .mdl-card__actions
              .title
                span
                  | デザイン

      .item
        a(href=`${worksPath}music/`)
          .card.mdl-card.mdl-shadow--2dp(style=
            `background-image: url(${rootPath}img/works/thumb/music.png);`
          )

            .mdl-card__title.mdl-card--expand
            .mdl-card__actions
              .title
                span
                  | 音楽

      .item
        a(href=`${worksPath}writing/`)
          .card.mdl-card.mdl-shadow--2dp(style=`
            background-image: url(${rootPath}img/works/thumb/writing.png);
          `)

            .mdl-card__title.mdl-card--expand
            .mdl-card__actions
              .title
                span
                  | 詩・執筆
